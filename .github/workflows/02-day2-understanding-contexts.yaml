name: "01- understanding contexts"
on: push
jobs:
  event-info-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Triger event-info-job
        run: echo "My trigger event is ${{github.event_name}}"
